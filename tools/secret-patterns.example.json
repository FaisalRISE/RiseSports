{
  "_comment": [
    "Template. Copy to tools/secret-patterns.local.json and put the REAL values in that copy.",
    "The .local.json file is gitignored and must never be committed — it is the one place the",
    "actual secrets are written down, and a scanner for secrets must not itself contain any.",
    "Patterns are matched as LITERAL text (git grep -F), not regular expressions."
  ],
  "patterns": [
    { "name": "Supabase anon JWT", "pattern": "PUT-THE-JWT-PREFIX-HERE" },
    { "name": "Organiser password (ADMIN_DEFAULT)", "pattern": "PUT-THE-PASSWORD-HERE" },
    { "name": "Supabase DB password", "pattern": "PUT-THE-DB-PASSWORD-HERE" }
  ]
}
